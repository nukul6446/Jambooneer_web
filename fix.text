| #      | Issue                                            | Fix                                                                                                                                                                                                                                                  |
| ------ | ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1**  | Product bottle branded "kaiyo" + wrong `<title>` | `document.title` now set to **"Jambooneer \| Pure Water. Honest Delivery."** via `useEffect`. Added a `// TODO` note: you still need to swap `product2.png` for real Jambooneer-branded packaging.                                                   |
| **2**  | Contact form doesn't submit                      | Added full React state (`formData`, `formStatus`), field validation, and visual feedback (loading / success / error messages). All inputs now have `name` and `id` attributes. Includes a `// TODO` to wire the `handleSubmit` to your real API/CRM. |
| **3**  | Duplicate testimonials visible                   | Expanded testimonials from **3 → 6 unique entries** so the marquee loop no longer shows identical cards side-by-side.                                                                                                                                |
| **4**  | Placeholder contact details                      | Moved address, phone, and email to top-level `BUSINESS_INFO` constants with clear `// TODO: Replace` markers. Easy to find and update before going live.                                                                                             |
| **5**  | "Order Now" button doesn't place an order        | Renamed nav CTA to **"Enquire Now"** so the label matches the actual behavior (scrolls to contact form).                                                                                                                                             |
| **6**  | Inconsistent arrow icons                         | Already consistent in source (all cards use `FiArrowUpRight`). No change needed.                                                                                                                                                                     |
| **7**  | "A Commitment Beyond the Bottle" greyed out      | Changed GSAP start opacity from `0.25` → `0.55`. Item now looks readable at rest while still animating in on scroll.                                                                                                                                 |
| **8**  | Stray circular cursor outline                    | Cursor starts at `opacity: 0` and only appears after first `mousemove`. Also fades out on `mouseleave`, preventing it from getting stuck mid-page during scroll.                                                                                     |
| **9**  | Missing accessible labels                        | Added `<label htmlFor="...">` (screen-reader only) to every form field. Mobile hamburger button now has `aria-label` and `aria-expanded`.                                                                                                            |
| **10** | Missing SEO / social metadata                    | `useEffect` injects `meta description`, Open Graph (`og:title`, `og:description`, `og:image`, `og:type`), and Twitter Card tags. Configurable via constants at the top of the file.                                                                  |
| **11** | Conflicting Tailwind font-size classes           | Removed redundant `text-sm` from footer tagline; kept only `text-[19px]`.                                                                                                                                                                            |
| **12** | Inconsistent eyebrow label sizing                | Standardized **all** section kickers to `text-[11px]` (hero was `10px`, others were `15px`).                                                                                                                                                         |
| **13** | SPA with no fallback                             | Added `<noscript>` block with basic content for no-JS users. Note: true SSR/prerendering requires build-level changes (e.g., Next.js or `react-snap`).                                                                                               |

 Before you deploy
Replace the bottle image (src/assets/product2.png) with real Jambooneer-branded photography.

Update BUSINESS_INFO constants with the real address and phone number.
9770674420
Wire the form in handleSubmit to your backend, email service (EmailJS, Resend, etc.), or CRM instead of the simulated setTimeout.
Update OG_IMAGE with your actual social-share image URL.